/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "avatars.githubusercontent.com",
            "lh3.googleusercontent.com",
            "samir-twitter-app.s3.ap-south-1.amazonaws.com",
            "www.google.com"
        ]
    }
};

export default nextConfig;
